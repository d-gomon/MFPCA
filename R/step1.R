step1_mFPCA <- function(mFData, landmark_var = NULL){
  #Takes multifunData input, de-means according to landmark_var if
  #specified, otherwise performs normal mFPCA while setting scores to zero
  #which cannot be calculated
  
}